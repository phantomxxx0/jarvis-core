/**
 * Categorizes the core architecture or purpose of an inference model.
 */
export enum ModelType {
  TEXT = 'TEXT',
  EMBEDDING = 'EMBEDDING',
  MULTIMODAL = 'MULTIMODAL',
  AUDIO = 'AUDIO',
  IMAGE_GENERATION = 'IMAGE_GENERATION',
  REASONING = 'REASONING',
}
